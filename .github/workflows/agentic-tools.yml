name: Agentic tool runtime

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/api/src/modules/evidence/**"
      - "apps/api/src/modules/classification/**"
      - "apps/api/src/modules/reconciliation/**"
      - "apps/api/src/modules/scan/**"
      - "apps/api/src/modules/legal-rule-catalog/**"
      - "packages/contracts/src/evidence/**"
      - "lcsp-python-workers/src/lcsp_workers/agentic_evidence/**"
      - "lcsp-python-workers/tests/agentic_evidence/**"
      - "scripts/check-agentic-tool-runtime.mjs"
      - ".github/workflows/agentic-tools.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime-integrity:
    name: Sprint 6 runtime integrity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Check API contract, handler, module and exposure wiring
        run: node scripts/check-agentic-tool-runtime.mjs

  worker-registry:
    name: Worker agentic registry
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lcsp-python-workers
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: lcsp-python-workers/pyproject.toml

      - name: Install worker dependencies
        run: python -m pip install -e ".[dev]"

      - name: Test fail-closed tool registry
        run: pytest tests/agentic_evidence/test_registry.py
