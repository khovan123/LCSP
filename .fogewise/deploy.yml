services:
  api:
    path: apps/api
    requires:
      - redis
      - rabbitmq

  web:
    path: apps/web
    route: /

  managed-deep-agent:
    path: deepagents
    requires:
      - redis
      - rabbitmq
