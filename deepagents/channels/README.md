# Managed Channels

This directory is reserved for Managed Deep Agents channel configuration.
Application tool code lives under `tools/`, while eval Harbor workspace files
live under `evals/tasks/` and `evals/scaffold/`.
